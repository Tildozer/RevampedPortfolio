import React from "react";

interface Props {}

const Footer = (props: Props) => {
  return (
    <div className="absolute bottom-0 w-screen flex justify-center items-center">
      Footer
    </div>
  );
};

export default Footer;
